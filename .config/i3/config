# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod1

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:monospace 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec termite
bindsym Mod5+Return exec termite

# kill focused window
bindsym $mod+Shift+q kill

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down

bindsym $mod+l focus up
bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+semicolon move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# switch to workspace
bindsym $mod+1 workspace 1: web
bindsym $mod+2 workspace 2: terms
bindsym $mod+3 workspace 3: media
bindsym $mod+4 workspace 4: files
bindsym $mod+5 workspace 5: dev
bindsym $mod+6 workspace 6: steam
bindsym $mod+7 workspace 7: game
bindsym $mod+8 workspace 8: misc
bindsym $mod+9 workspace 9: nine
bindsym $mod+0 workspace 10: ten

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace 1: web
bindsym $mod+Shift+2 move container to workspace 2: terms
bindsym $mod+Shift+3 move container to workspace 3: media
bindsym $mod+Shift+4 move container to workspace 4: files
bindsym $mod+Shift+5 move container to workspace 5: dev
bindsym $mod+Shift+6 move container to workspace 6: steam
bindsym $mod+Shift+7 move container to workspace 7: game
bindsym $mod+Shift+8 move container to workspace 8: misc
bindsym $mod+Shift+9 move container to workspace 9: nine
bindsym $mod+Shift+0 move container to workspace 10: ten

# reload the configuration file
# bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
    # These bindings trigger as soon as you enter the resize mode

    # Pressing left will shrink the window’s width.
    # Pressing right will grow the window’s width.
    # Pressing up will shrink the window’s height.
    # Pressing down will grow the window’s height.
    bindsym j resize shrink width 10 px or 10 ppt
    bindsym k resize grow height 10 px or 10 ppt
    bindsym l resize shrink height 10 px or 10 ppt
    bindsym semicolon resize grow width 10 px or 10 ppt

    # same bindings, but for the arrow keys
    bindsym Left resize shrink width 10 px or 10 ppt
    bindsym Down resize grow height 10 px or 10 ppt
    bindsym Up resize shrink height 10 px or 10 ppt
    bindsym Right resize grow width 10 px or 10 ppt

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

#my keybinds
bindsym $mod+d exec ~/.config/i3/rofi.sh
bindsym $mod+x exec ~/.config/i3/lock.sh
bindsym $mod+c exec google-chrome-unstable --force-device-scale-factor=1.25
bindsym $mod+Shift+c exec google-chrome-unstable --force-device-scale-factor=1.25 --incognito
bindsym $mod+m exec google-play-music-desktop-player --force-device-scale-factor=1.25

#media keys
bindsym XF86AudioRaiseVolume exec pulsemixer --unmute +5 && pulsemixer --change-volume +5
bindsym XF86AudioLowerVolume exec pulsemixer --change-volume -5
bindsym XF86AudioMute exec pulsemixer --mute
#control+media
bindsym Control+XF86AudioRaiseVolume exec pulsemixer --change-volume +1 && pulsemixer --unmute
bindsym Control+XF86AudioLowerVolume exec pulsemixer --change-volume -1

#brightness
bindsym XF86MonBrightnessUp exec xbacklight -inc 5
bindsym XF86MonBrightnessDown exec xbacklight -dec 5
#cotrol+brightness
bindsym Control+XF86MonBrightnessUp exec xbacklight -inc 1
bindsym Control+XF86MonBrightnessDown exec xbacklight -dec 1
#mod+brightness
bindsym $mod+XF86MonBrightnessDown exec xbacklight -set 1

# Start conky.sh to display a workspace bar
bar {
	status_command ~/.config/i3/conky.sh
	tray_output none
	position top
	mode dock
	font pango:inconsolata-g 5
	colors {
	    background #181818
	   	statusline #605c5a
		focused_workspace	#181818	#AB4642	#f8f8f8
  		active_workspace	#181818 #161616	#ffffff
  		inactive_workspace	#181818 #585858	#ffffff
		urgent_workspace  	#181818 #585858	#ffffff
	}
}

#no window borders
new_window none

#set Termite border pixel size
for_window [class="Termite"] border pixel 1
#focused winddow borders for Termite
client.focused #AB4642 #AB4642 #AB4642 #AB4642

#gaps for normal windows
gaps inner 10
gaps outer 10

#special gaps
workspace "1: web" gaps outer 0
workspace "1: web" gaps inner 0
workspace "2: terms" gaps outer 150

#assign programs to workspaces and move to that workspace
for_window [class="Google-chrome-unstable"] move container to workspace "1: web", workspace "1: web"
for_window [class="Termite"] move container to workspace "2: terms", workspace "2: terms"
for_window [class="mpv"] move container to workspace "3: media", workspace "3: media"
for_window [class="Google Play Music Desktop Player"] move container to workspace "3: media", workspace "3: media"
for_window [class="transmission-gtk"] move container to workspace "3: media", workspace "3: media"
for_window [class="Thunar"] move container to workspace "4: files", workspace "4: files"
for_window [class="Subl3"] move container to workspace "5: dev", workspace "5: dev"
for_window [title="nano"] move container to workspace "5: dev", workspace "5: dev"
for_window [title="nvim"] move container to workspace "5: dev", workspace "5: dev"
for_window [class="Steam"] move container to workspace "6: steam"
for_window [title="Hexcells"] move container to workspace "7: game", workspace "7: game"
for_window [title="Gunpoint"] move container to workspace "7: game", workspace "7: game"
for_window [title="MiniMetro"] move container to workspace "7: game", workspace "7: game"
for_window [title="Shattered Pixel Dungeon"] move container to workspace "7: game", workspace "7: game"
for_window [title="Osmos"] move container to workspace "7: game", workspace "7: game"
for_window [title="pyrogenesis"] move container to workspace "7: game", workspace "7: game"

#exec things: compositor, background, redshift, batwarn
exec --no-startup-id compton --config ~/.config/i3/compton.conf
exec --no-startup-id ~/.fehbg
exec --no-startup-id pkill redshift || sleep 3 && redshift -l 53.3756707:-2.3587521 -t 6500:3000
exec --no-startup-id batwarn
